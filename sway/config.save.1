# Default config for sway
#
# Copy this to ~/.config/sway/config and edit it to your liking.
#
# Read `man 5 sway` for a complete reference.

### Variables
#

# Logo key. Use Mod1 for Alt.
set $mod Mod4

# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

# Terminal Emulator
set $term alacritty
# Your preferred application launcher
set $menu rofi -show drun

# You can get the names of your outputs by running: swaymsg -t get_outputs
output DP-1 resolution 1920x1080@144hz position 0,0
output DP-2 resolution 1920x1080@144hz position 1920,0

# For locking the screen, locks after 300 seconds, turns display off 300 seconds later.
exec swayidle -w \
          timeout 300 'swaylock -f -c 000000' \
          timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
          before-sleep 'swaylock -f -c 000000'

exec waybar --config /home/amilie/.config/waybar/config.json
    
#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.
#bar {
#	status_command waybar
#    }
#}

include /etc/sway/config.d/*
include /home/amilie/.config/sway/appearance.conf
include /home/amilie/.config/sway/binds.conf
